<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately plain: RemoteViews supports only a small set of views, and a
  widget that fails to inflate simply never appears. The colours are set at
  runtime from the app's active theme — see SaydleWidgetProvider.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/saydle_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F7CAC5">

    <TextView
        android:id="@+id/saydle_widget_wordmark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SAYDLE"
        android:textSize="10sp"
        android:textStyle="bold"
        android:letterSpacing="0.16"
        android:textColor="#FF6F61" />

    <TextView
        android:id="@+id/saydle_widget_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        android:lineSpacingExtra="3sp"
        android:textColor="#38223A"
        android:maxLines="5"
        android:ellipsize="end"
        android:text="I am allowed to start small." />

</LinearLayout>
